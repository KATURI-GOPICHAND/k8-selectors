apiVersion: v1
kind: Pod
metadata:
  name: nginx-tolerated
  labels:
    env: test
spec:
  containers:
  - name: nginx
    image: nginx
  # asking for VIP room
  nodeSelector:
    project: roboshop
  # showing booking confirmation
  tolerations:
  - key: "roboshop"
    operator: "Equal"
    value: "roboshop"
    effect: "NoExecute"
